defaults:
  - filepaths: filepaths
  - _self_

dxgb:
  _target_: DORA_XGB.DORA_XGB.feasibility_classifier
  cofactor_positioning: add_concat

mfper:
  _target_: ergochemics.similarity.MorganFingerprinter
  radius: 3
  length: 2048
  mol_featurizer:
    _target_: ergochemics.similarity.MolFeaturizer

rc_dist_ub: null # Upperbound on distance from RC used for fingerprint
use_rc_in_fingerprints: true
filter_by_rc: true # If true, reactions with different RCs get similarity = 0
processes: 5
casp_study: ???

hydra:
  output_subdir: .analyze_structures
  run:
    dir: ${filepaths.processed_data}/${...casp_study}
  job:
    chdir: True
  sweep:
    dir: ${filepaths.processed_data}/${...casp_study}
    subdir: .