defaults:
  - filepaths: filepaths
  - _self_

casp_study: ???

hydra:
  output_subdir: .draw_reactions
  run:
    dir: ${filepaths.processed_data}/${...casp_study}
  job:
    chdir: True
  sweep:
    dir: ${filepaths.processed_data}/${...casp_study}
    subdir: .