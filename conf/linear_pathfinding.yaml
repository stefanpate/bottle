defaults:
  - filepaths: filepaths
  - _self_

sources: sources.txt
targets: targets.txt
helpers: helpers.txt
am_rxns: am_rxns.parquet
expansion: ???
casp_study: ???
expansion_extract: ${filepaths.interim_data}/expansions/${.expansion}
max_depth: ???
pnmc_lb: 0.0
rnmc_lb: 0.0
source_augmented_pnmc_lb: 0.0

hydra:
  run:
    dir: ${filepaths.processed_data}/${...casp_study}
  job:
    chdir: True
  sweep:
    dir: ${filepaths.processed_data}/${...casp_study}
    subdir: .